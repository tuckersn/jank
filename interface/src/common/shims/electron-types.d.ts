import * as ElectronType from "electron";
import { ipcRenderer, shell, remote } from "electron";

export declare type ElectronType = typeof ElectronType;
export declare type IpcRendererType = typeof ipcRenderer;
export declare type ShellType = typeof shell;
export declare type RemoteType = typeof remote;